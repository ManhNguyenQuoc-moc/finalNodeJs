<!DOCTYPE html>
<html lang="vi">
<head>
  <meta charset="UTF-8" />
  <title><%= title %> | E-Shop</title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="min-h-screen bg-slate-100 flex items-center justify-center">
  <div class="w-full max-w-md mx-4">
    <div class="bg-white rounded-3xl shadow-xl px-8 py-10">
      <!-- Logo -->
      <div class="flex justify-center mb-6">
        <img src="/images/logo-shop.png" alt="E-Shop" class="h-12 object-contain"
             onerror="this.style.display='none'" />
      </div>

      <div class="flex flex-col items-center text-center space-y-2 mb-6">
        <h1 class="text-xl font-semibold text-gray-900">Quên mật khẩu</h1>
        <p class="text-xs text-gray-500">
          Nhập email đã đăng ký để nhận link đặt lại mật khẩu.
        </p>
      </div>

      <% if (error) { %>
        <p class="text-xs text-red-500 mb-3 text-center"><%= error %></p>
      <% } %>

      <% if (success) { %>
        <p class="text-xs text-emerald-600 mb-3 text-center"><%= success %></p>
      <% } %>

      <form method="POST" action="/forgot-password" class="space-y-4 text-left">
        <div>
          <label for="email" class="block text-sm font-medium text-gray-700 mb-1">
            Email
          </label>
          <input id="email" name="email" type="email" required
                 class="block w-full h-11 rounded-xl border border-gray-300 px-3 text-sm text-gray-900 placeholder-gray-400 focus:outline-none focus:ring-2 focus:ring-black focus:border-black"
                 placeholder="Nhập email của bạn" value="<%= email || '' %>" />
        </div>

        <button type="submit"
                class="w-full inline-flex justify-center items-center mt-2 px-4 py-2.5 rounded-full bg-black text-white text-sm font-semibold hover:bg-gray-900 transition">
          Gửi link đặt lại mật khẩu
        </button>
      </form>

      <div class="mt-6 text-center">
        <a href="/login" class="text-xs text-gray-500 hover:text-gray-700">
          Quay lại đăng nhập
        </a>
      </div>
    </div>
  </div>
</body>
</html>
