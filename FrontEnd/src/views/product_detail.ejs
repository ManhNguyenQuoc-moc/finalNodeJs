  <!DOCTYPE html>
  <html dir="ltr" lang="zxx">
  <style>
      .chat-bubble{background:#f6f7f9;border-radius:12px;padding:10px 12px}
      .chat-meta{font-size:12px;color:#6b7280}
      .comment-item{margin-bottom:12px}
      .comment-item .children{margin-left:20px;margin-top:8px}
      .btn-xxs{padding:2px 8px;font-size:12px;line-height:20px}
      .like-btn{background:transparent;border:0;cursor:pointer}
      .like-btn .heart{font-size:16px;vertical-align:-2px;margin-right:4px}
      .reply-form textarea{min-height:70px}
      .star, .rate-star {
      font-style: normal;
      font-size: 18px;
      line-height: 1;
      color: #bbb;
      cursor: default;
      transition: transform .08s ease, color .12s ease;
      user-select: none;
    }
    .star.filled, .rate-star.filled { color: #f5a623; }
    .rating-box .rate-star { cursor: pointer; font-size: 22px; margin-right: 2px; }
    .rating-box .rate-star:hover { transform: scale(1.1); }
    .cmt-item:last-child { border-bottom: 0 !important; }

  /* Q&A */
    .qa-card{background:#fff;border:1px solid #eef2f7;border-radius:14px;
            box-shadow:0 1px 2px rgba(16,24,40,.04);padding:18px}
    .qa-hero{display:flex;gap:18px;align-items:center;background:#f7f8fb;
            border-radius:14px;padding:18px}
    .qa-hero__img{width:110px;flex:0 0 110px}
    .qa-hero__img img{width:100%;height:auto}
    .qa-title{font-weight:800;font-size:28px;margin-bottom:8px}
    .qa-desc{color:#6b7280}
    .qa-input{display:flex;gap:14px;margin-top:14px}
    .qa-input .form-control{height:48px}
    .btn-ask{background:#e8071b;color:#fff;border:0;height:48px;border-radius:10px;
            padding:0 22px;font-weight:700}
    .btn-ask:hover{filter:brightness(.95)}
    @media (max-width:768px){
      .qa-hero{flex-direction:column;align-items:flex-start}
      .qa-input{flex-direction:column}
      .btn-ask{width:100%}
      .qa-hero__img{width:82px;flex:0 0 82px}
    }

    /* Subnav */
  .pdp-subnav { background:#fff; z-index: 2; border-bottom:1px solid #eee; }
  .pdp-subnav__list { display:flex; gap:18px; padding:10px 0; margin:0; list-style:none; overflow:auto; }
  .pdp-subnav__list a { display:inline-block; padding:8px 10px; border-radius:8px; font-weight:600; color:#374151; text-decoration:none; }
  .pdp-subnav__list a:hover, .pdp-subnav__list a.active { background:#f1f5f9; color:#111827; }

  /* Section card */
  .pdp-section { scroll-margin-top: 80px; }
  .section-title { font-size:20px; font-weight:700; }
  .card { border:1px solid #eef2f7; border-radius:14px; box-shadow:0 1px 2px rgba(16,24,40,.04); }

  /* Feature tiles */
  .feature-tile { border:1px dashed #e5e7eb; border-radius:12px; padding:12px; background:#fafafa; }
  .ft-title { font-weight:600; margin-bottom:4px; }

  /* Stars */
  .rating-large .star { font-size:24px; color:#bbb; }
  .star.filled, .rate-star.filled { color:#f59e0b; }

  /* Swatch polish */
  .custom-swatch-container .custom-swatch-color { width:32px; height:32px; border-radius:8px; border:1px solid #e5e7eb; margin-right:8px; }
  .custom-swatch-container .custom-swatch-color.swatch_active { outline:2px solid #111827; }

  /* Qty control */
  .qty-control { border:1px solid #e5e7eb; border-radius:10px; overflow:hidden; }
  .qty-control__reduce, .qty-control__increase { width:38px; cursor:pointer; }




  </style>


  <!-- Mirrored from uomo-html.flexkitux.com/Demo3/product2_variable.html by HTTrack Website Copier/3.x [XR&CO'2014], Sat, 19 Oct 2024 06:54:03 GMT -->
  <%- include('partials/head') %>

  <body>

  <%- include('partials/header') %>

  <div id="notificationModal" class="notification-modal">
      <div class="notification-modal-content">
          <span class="close-button" id="closeModal">&times;</span>
          <p id="modalMessage"></p>
      </div>
  </div>

  <main>
      <div class="mb-md-1 pb-md-3"></div>
      <section class="product-single container">
          <div class="row">
              <div class="col-lg-7">
                  <div class="product-single__media" data-media-type="vertical-thumbnail">
                      <div class="product-single__image">
                          <div class="swiper-container">
                              <div class="swiper-wrapper" id="main-image-container">
                                  <% allImages.forEach(function(imageUrl) { %>
                                      <div class="swiper-slide product-single__image-item">
                                          <img loading="lazy" class="h-auto" src="<%= imageUrl %>" width="674" height="674" alt="<%= product.name %>">
                                          <img class="category-banner__frame"
                                            src="<%= product.productStatus.statusName === 'Bán chạy' 
                                                      ? '/mixishop/images/frame_banchay.png' 
                                                      : (product.productStatus.statusName === 'Trending' 
                                                          ? '/mixishop/images/frame_trending.png' 
                                                          : '/mixishop/images/frame_new.png') %>"
                                            alt="Status Frame">

                                      </div>
                                  <% }) %>
                              </div>
                              <div class="swiper-button-prev"><svg width="7" height="11" viewBox="0 0 7 11" xmlns="http://www.w3.org/2000/svg"><use href="#icon_prev_sm" /></svg></div>
                              <div class="swiper-button-next"><svg width="7" height="11" viewBox="0 0 7 11" xmlns="http://www.w3.org/2000/svg"><use href="#icon_next_sm" /></svg></div>
                          </div>
                      </div>
                      <div class="product-single__thumbnail">
                          <div class="swiper-container">
                              <div class="swiper-wrapper" id="thumbnail-image-container">
                                  <% allImages.forEach(function(imageUrl) { %>
                                      <div class="swiper-slide product-single__image-item">
                                          <img loading="lazy" class="h-auto" src="<%= imageUrl %>" width="104" height="104" alt="<%= product.name %>" />
                                      </div>
                                  <% }) %>
                              </div>
                          </div>
                      </div>
                  </div>
              </div>
              <div class="col-lg-5">
                  <div class="d-flex justify-content-between mb-4 pb-md-2">
                      <div class="breadcrumb mb-0 d-none d-md-block flex-grow-1">
                          <a href="#" class="menu-link menu-link_us-s text-uppercase fw-medium">Trang chủ</a>
                          <span class="breadcrumb-separator menu-link fw-medium ps-1 pe-1">/</span>
                          <a href="#" class="menu-link menu-link_us-s text-uppercase fw-medium"><%= product.category?.name %></a>
                      </div>
                  </div>
                  <h1 class="product-single__name"><%= product.name %></h1>

                  <div class="d-flex align-items-center flex-wrap gap-3 mb-2">
                  <div class="product-single__price mb-0">
                    <span class="current-price">
                      <%= formatPrice(product.display_price || product.price) %>
                    </span>
                  </div>

                  <div class="small text-muted">
                    Thương hiệu: <strong><%= product.brand?.name || 'Đang cập nhật' %></strong>
                  </div>

                  </div>

                  <!-- Rating tổng quan -->
                  <div class="mb-3">
                    <% const __avg = Number(product.avg_rating || 0);
                      const __cnt = Number(product.rating_count || 0);
                      const __rounded = Math.round(__avg);
                    %>
                    <span class="rating-summary align-middle">
                      <% for (let i = 1; i <= 5; i++) { %>
                        <i class="star <%= i <= __rounded ? 'filled' : '' %>">★</i>
                      <% } %>
                    </span>
                    <span class="small text-muted ms-2">
                      <%= __avg.toFixed(1) %>/5 • <%= __cnt %> lượt đánh giá
                    </span>
                  </div>


                  <div class="product-single__short-desc">
                  <!-- giữ xuống dòng trong description -->
                  <p style="white-space: pre-line;"><%= product.short_description %></p>
                  </div>

                  <form name="addtocart-form" method="post" action="/add-to-cart">
                    <div class="product-single__swatches">

                      <!-- SIZE -->
                      <div class="product-swatch text-swatches">
                        <label class="font-weight-bold">Kích thước</label>
                        <div class="swatch-list d-flex flex-wrap">
                          <% (productSizes || []).forEach(function(size, i){ %>
                            <input
                              type="radio"
                              name="size"
                              id="swatch-size-<%= i %>"
                              value="<%= size.sku %>"                  
                              data-size-id="<%= size.size_id %>"      
                              data-size-name="<%= size.name %>"  
                              <%= i === 0 ? 'checked' : '' %>
                              class="d-none"
                              onchange="updateSizeSelection(this)"      
                            />
                            <label
                              class="swatch js-swatch"
                              for="swatch-size-<%= i %>"
                              aria-label="<%= size.name %>"
                              data-bs-toggle="tooltip"
                              data-bs-placement="top"
                              title="<%= size.name %>"
                              style="cursor:pointer;">
                              <span><%= size.name %></span>
                            </label>
                          <% }) %>
                        </div>
                        <a href="#" class="sizeguide-link mt-2" data-bs-toggle="modal" data-bs-target="#sizeGuide">
                          Bảng kích thước
                        </a>
                      </div>

                      <!-- COLOR -->
                      <div class="product-swatch color-swatches">
                        <label>Màu</label>
                        <div class="swatch-list d-flex align-items-center mt-1 custom-swatch-container">
                          <% (product.colors || []).forEach(function(productColor){ 
                              const title = productColor.color_id?.color_name || productColor.color || 'Màu';
                              const img0 = (productColor.imageUrls && productColor.imageUrls[0]) || '';
                              const colorCode = productColor.color_code || '';
                              const styleStr = img0 
                                ? `background-image:url('${img0}'); background-size:cover;`
                                : (colorCode ? `background-color:${colorCode};` : '');
                          %>
                            <a href="#"
                              class="swatch-color custom-swatch-color"
                              style="<%= styleStr %>"
                              data-images="<%= (productColor.imageUrls || []).join(',') %>"
                              data-value="<%= title %>"
                              data-color-id="<%= productColor.color_id?._id || '' %>"  
                              onclick="changeColor(this); updateColorSelection(this); return false;"
                              title="<%= title %>">
                            </a>

                          <% }) %>
                        </div>
                      </div>


                      <!-- Debug pick (tạm thời để kiểm tra) -->
                      <div class="small text-muted mt-2" id="debugPick">
                        Đang chọn: <strong>Màu</strong> <span id="dbgColor">—</span> / 
                        <strong>Size</strong> <span id="dbgSize">—</span>
                      </div>


                      <!-- Hidden fields -->
                      <input type="hidden" name="sizeName"   id="sizeName"     value="">
                      <input type="hidden" name="colorName"  id="selectedColor" value="">
                      <input type="hidden" name="product_id" id="productId"    value="<%= product._id %>">
                      <input type="hidden" name="variant_sku" id="variantSku"  value=""> <!-- có/không đều được -->
                      <input type="hidden" name="size_id"     id="sizeId"      value="">
                      <input type="hidden" name="color_id"    id="colorId"     value="">



                    </div>

                    <!-- Add to cart -->
                    <div class="product-single__addtocart">
                      <div class="qty-control position-relative">
                        <input type="number" name="quantity" value="1" min="1" class="qty-control__number text-center">
                        <div class="qty-control__reduce">-</div>
                        <div class="qty-control__increase">+</div>
                      </div>
                      <button type="submit" class="btn btn-primary" data-aside="cartDrawer">
                        Thêm vào giỏ hàng
                      </button>
                    </div>
                  </form>

                  <div class="product-single__addtolinks">
                      <a href="#" class="menu-link menu-link_us-s add-to-wishlist"><svg width="16" height="16" viewBox="0 0 20 20" fill="none" xmlns="http://www.w3.org/2000/svg"><use href="#icon_heart" /></svg><span>Thêm yêu thích</span></a>
                      <share-button class="share-button">
                          <button class="menu-link menu-link_us-s to-share border-0 bg-transparent d-flex align-items-center">
                              <svg width="16" height="19" viewBox="0 0 16 19" fill="none" xmlns="http://www.w3.org/2000/svg"><use href="#icon_sharing" /></svg>
                              <span>Chia sẻ</span>
                          </button>
                          <details id="Details-share-template__main" class="m-1 xl:m-1.5" hidden="">
                              <summary class="btn-solid m-1 xl:m-1.5 pt-3.5 pb-3 px-5">+</summary>
                              <div id="Article-share-template__main" class="share-button__fallback flex items-center absolute top-full left-0 w-full px-2 py-4 bg-container shadow-theme border-t z-10">
                                  <div class="field grow mr-4">
                                      <label class="field__label sr-only" for="url">Link</label>
                                      <input type="text" class="field__input w-full" id="url" value="https://uomo-crystal.myshopify.com/blogs/news/go-to-wellness-tips-for-mental-health" placeholder="Link" onclick="this.select();" readonly="">
                                  </div>
                                  <button class="share-button__copy no-js-hidden">
                                      <svg class="icon icon-clipboard inline-block mr-1" width="11" height="13" fill="none" xmlns="http://www.w3.org/2000/svg" aria-hidden="true" focusable="false" viewBox="0 0 11 13">
                                          <path fill-rule="evenodd" clip-rule="evenodd" d="M2 1a1 1 0 011-1h7a1 1 0 011 1v9a1 1 0 01-1 1V1H2zM1 2a1 1 0 00-1 1v9a1 1 0 001 1h7a1 1 0 001-1V3a1 1 0 00-1-1H1zm0 10V3h7v9H1z" fill="currentColor"></path>
                                      </svg>
                                      <span class="sr-only">Copy link</span>
                                  </button>
                              </div>
                          </details>
                      </share-button>
                      <script src="/mixishop/js/details-disclosure.js" defer="defer"></script>
                      <script src="/mixishop/js/share.js" defer="defer"></script>
                  </div>
                  <div class="product-single__meta-info">
                      <div class="meta-item">
                          <label>Sản phẩm: </label>
                          <span><%= product.category?.name %></span>
                      </div>
                  </div>
              </div>
          </div>
          <div class="product-single__details-tab">
              <ul class="nav nav-tabs" id="myTab1" role="tablist">
                  <li class="nav-item" role="presentation">
                      <a class="nav-link nav-link_underscore active" id="tab-description-tab" data-bs-toggle="tab" href="#tab-description" role="tab" aria-controls="tab-description" aria-selected="true">Khuyến mãi</a>
                  </li>
                  <li class="nav-item" role="presentation">
                      <a class="nav-link nav-link_underscore" id="tab-additional-info-tab" data-bs-toggle="tab" href="#tab-additional-info" role="tab" aria-controls="tab-additional-info" aria-selected="false">Thông tin sản phẩm</a>
                  </li>
              </ul>
              <div class="tab-content">
                  <div class="tab-pane fade show active" id="tab-description" role="tabpanel" aria-labelledby="tab-description-tab">
                      <div class="product-single__description">
                          <div class="product-promotion rounded-sm" id="ega-salebox">
                              <h3 class="product-promotion__heading rounded-sm d-inline-flex align-items-center">
                                  KHUYẾN MÃI - ƯU ĐÃI
                              </h3>
                              <ul class="promotion-box">
                                  <li>Chuyển khoản với đơn hàng từ 500k trở lên (Bắt buộc)</li>
                                  <li>Đồng giá ship toàn quốc 30k</li>
                                  <li>Hỗ trợ trả lời thắc mắc qua fanpage chính thức</li>
                                  <li>Khuyến mãi trực tiếp trên giá sản phẩm</li>
                                  <li>Đổi trả nếu sản phẩm lỗi bất kì</li>
                              </ul>
                          </div>
                      </div>
                  </div>
                  <div class="tab-pane fade" id="tab-additional-info" role="tabpanel" aria-labelledby="tab-additional-info-tab">
                      <div class="product-single__addtional-info">
                          <!-- MÔ TẢ DÀI (HTML) -->
                          <article class="prose" style="white-space: normal;">
                          <%- (product.long_description || product.detailDescription || ('<p>'+ (product.description||'').replace(/\n/g,'<br>') +'</p>')) %>
                          </article>

                          <!-- Khối “thông số nhanh” (nếu có) -->
                          <% if (product.materials || product.care) { %>
                          <div class="row g-3 mt-3">
                              <% if (product.materials) { %>
                              <div class="col-md-6">
                                  <div class="feature-tile">
                                  <div class="ft-title">Chất liệu</div>
                                  <div class="ft-body"><%= product.materials %></div>
                                  </div>
                              </div>
                              <% } %>
                              <% if (product.care) { %>
                              <div class="col-md-6">
                                  <div class="feature-tile">
                                  <div class="ft-title">Bảo quản</div>
                                  <div class="ft-body"><%= product.care %></div>
                                  </div>
                              </div>
                              <% } %>
                          </div>
                          <% } %>

                          <!-- Size & Màu (tóm tắt gọn) -->
                          <div class="item mt-3">
                          <label class="h6">Kích thước</label>
                          <span>
                              <% productSizes.forEach(function(size, index) { %>
                              <span><%= size.name  %></span><%= index < productSizes.length - 1 ? ', ' : '' %>
                              <% }) %>
                          </span>
                          </div>
                          <div class="item">
                          <label class="h6">Màu</label>
                          <span>
                              <% product.colors.forEach(function(c, index) { %>
                                <span><%= c.color_id?.color_name || 'Màu' %></span><%= index < product.colors.length - 1 ? ', ' : '' %>
                              <% }) %>
                          </span>
                          </div>
                      </div>
                      </div>

              </div>
          </div>

          <nav class="pdp-subnav sticky-top container mt-3">
              <ul class="pdp-subnav__list">
                  <li><a href="#section-rating">Đánh giá</a></li>
                  <li><a href="#section-comments">Bình luận</a></li>
              </ul>
          </nav>



          <!-- ========== ĐÁNH GIÁ & BÌNH LUẬN (Realtime) ========== -->
  <section id="section-rating" class="container pdp-section">
    <div class="card p-4">
      <h3 class="section-title mb-3">Đánh giá sản phẩm</h3>

      <% const _avg = Number(product.avg_rating || 0);
        const _count = Number(product.rating_count || 0);
        const _rounded = Math.round(_avg);
      %>

      <div class="row g-4 align-items-center">
        <!-- Cột tổng điểm -->
        <div class="col-md-4">
          <div class="display-4 fw-bold">
            <span><%= _avg.toFixed(1) %></span><span class="fs-4">/5</span>
          </div>
          <div class="mt-1">
            <% for (let i=1;i<=5;i++){ %>
              <i class="star <%= i<=_rounded ? 'filled' : '' %>">★</i>
            <% } %>
          </div>
          <div class="small text-muted mt-2"><%= _count %> lượt đánh giá</div>
        </div>

        <!-- (Tuỳ chọn) bạn có thể bỏ 2 cột còn lại hoặc để nội dung marketing -->
        <div class="col-md-8">
          <div class="text-muted">
            Sản phẩm được người dùng đánh giá trung bình <strong><%= _avg.toFixed(1) %>/5</strong>.
          </div>
        </div>
      </div>

      <hr class="my-3">

      <!-- Khối “Chấm sao của bạn” có thể giữ nguyên UI, chỉ bỏ phụ thuộc biến server -->
      <div class="mb-2">Chấm sao của bạn:</div>
      <div id="ratingBox" class="rating-box" data-current="0">
        <% for (let i = 1; i <= 5; i++) { %>
          <i class="rate-star" data-star="<%= i %>">★</i>
        <% } %>
      </div>
      <div class="small text-muted mt-2">Nhấp vào số sao để đánh giá</div>
    </div>
  </section>


  <div class="container">
    <div class="d-flex flex-wrap gap-2 mb-3" id="reviewFilters">
      <button class="btn btn-sm btn-outline-secondary active" data-star="0">Tất cả</button>
      <button class="btn btn-sm btn-outline-secondary" data-star="5">5 sao</button>
      <button class="btn btn-sm btn-outline-secondary" data-star="4">4 sao</button>
      <button class="btn btn-sm btn-outline-secondary" data-star="3">3 sao</button>
      <button class="btn btn-sm btn-outline-secondary" data-star="2">2 sao</button>
      <button class="btn btn-sm btn-outline-secondary" data-star="1">1 sao</button>
    </div>
  </div>




  <section id="section-qa" class="container pdp-section">
    <div class="qa-card">
      <div class="qa-hero">
        <div class="qa-hero__img">
          <img src="/mixishop/images/mascot.png" alt="Mascot">
        </div>
        <div class="flex-grow-1">
          <div class="qa-title">Hãy đặt câu hỏi cho chúng tôi</div>
          <div class="qa-desc">
            Chúng tôi phản hồi trong ~1 giờ. Nếu câu hỏi sau 22h, sẽ trả lời sáng hôm sau.
          </div>
          <form id="qaForm" class="qa-input" onsubmit="return false;">
            <input type="text" class="form-control" id="qaMessage" placeholder="Viết câu hỏi của bạn tại đây">
            <button class="btn-ask" id="btnAsk">Gửi câu hỏi ✈️</button>
          </form>
        </div>
      </div>

      <!-- Danh sách Q&A -->
      <div class="mt-3">
        <ul id="qaList" class="list-unstyled m-0">
          <% const _qa = ((typeof reviews !== 'undefined' && Array.isArray(reviews)) ? reviews : []).filter(x => !x.parentId); %>
          <% if(!_qa.length){ %>
            <li class="text-muted p-3">Chưa có câu hỏi nào. Hãy là người đầu tiên!</li>
          <% } %>
          <% _qa.forEach(function(q){ 
              const liked = !!(q.likedBy && likerId && q.likedBy[likerId]);
              const children = (reviews||[]).filter(a=>a.parentId===q.id);
          %>
            <li class="comment-item p-3 border-bottom" data-id="<%= q.id %>">
              <div class="d-flex justify-content-between">
                <div>
                  <strong><%= q.user||'Khách' %></strong>
                  <span class="small text-muted">• <%= new Date(q.createdAt).toLocaleString('vi-VN') %></span>
                </div>
              </div>
              <div class="mt-1"><%= q.message %></div>
              <div class="d-flex align-items-center gap-3 mt-2">
                <button class="btn btn-link btn-xxs p-0 reply-btn" data-id="<%= q.id %>">Phản hồi</button>
                <button class="like-btn" data-id="<%= q.id %>">
                  <span class="heart"><%= liked?'❤️':'🤍' %></span>
                  <span class="likes-count"><%= q.likes||0 %></span>
                </button>
              </div>

              <form class="reply-form mt-2 d-none" data-parent="<%= q.id %>">
                <textarea class="form-control form-control-sm mb-2" name="message" placeholder="Viết trả lời..." required></textarea>
                <button class="btn btn-primary btn-xxs">Gửi</button>
              </form>

              <ul class="children list-unstyled ms-3 mt-2">
                <% if(children.length>2){ %>
                  <li><a href="#" class="toggle-replies small text-danger" data-id="<%= q.id %>">Xem thêm <%= children.length %> phản hồi</a></li>
                <% } %>
                <% children.forEach(function(a,i){ 
                    const likedA = !!(a.likedBy && likerId && a.likedBy[likerId]);
                %>
                  <li class="p-2 bg-light rounded mb-2 reply-item <%= i>1 ? 'd-none extra-reply-'+q.id : '' %>" data-id="<%= a.id %>">
                    <div><strong><%= a.user||'Khách' %></strong> 
                      <span class="small text-muted">• <%= new Date(a.createdAt).toLocaleString('vi-VN') %></span>
                    </div>
                    <div class="mt-1"><%= a.message %></div>
                    <div class="d-flex align-items-center gap-3 mt-2">
                      <button class="like-btn" data-id="<%= a.id %>">
                        <span class="heart"><%= likedA?'❤️':'🤍' %></span>
                        <span class="likes-count"><%= a.likes||0 %></span>
                      </button>
                    </div>
                  </li>
                <% }) %>
              </ul>
            </li>
          <% }) %>
        </ul>
      </div>
    </div>
  </section>

      </section>
      <section id="section-related" class="products-carousel container" style="margin-top: 10px;">
    <h2 class="h3 mb-4 pb-xl-2 mb-xl-4">
      Liên quan đến <span><%= product?.category?.name || 'sản phẩm này' %></span>
    </h2>

    <div id="related_products" class="position-relative">
      <div
        class="swiper-container js-swiper-slider"
        data-settings='{
          "autoplay": false,
          "slidesPerView": 4,
          "slidesPerGroup": 4,
          "effect": "none",
          "loop": true,
          "pagination": {
            "el": "#related_products .products-pagination",
            "type": "bullets",
            "clickable": true
          },
          "navigation": {
            "nextEl": "#related_products .products-carousel__next",
            "prevEl": "#related_products .products-carousel__prev"
          },
          "breakpoints": {
            "320":  { "slidesPerView": 2, "slidesPerGroup": 2, "spaceBetween": 14 },
            "768":  { "slidesPerView": 3, "slidesPerGroup": 3, "spaceBetween": 24 },
            "992":  { "slidesPerView": 4, "slidesPerGroup": 4, "spaceBetween": 30 }
          }
        }'
      >
        <div class="swiper-wrapper">
          <% (products || []).slice(0, Math.min((products || []).length, 10)).forEach(function(item){ %>
            <div class="swiper-slide product-card product-card_style3">
              <div class="pc__img-wrapper position-relative">
                <a href="/product_detail/<%= item.id || item._id %>">
                  <img
                    loading="lazy"
                    src="<%= (item.colors?.[0]?.imageUrls?.[0]) || '/images/default.png' %>"
                    width="258" height="313"
                    alt="<%= item.name %>"
                    class="pc__img"
                  >
                  <img
                    loading="lazy"
                    src="<%= (item.colors?.[0]?.imageUrls?.[1]) || (item.colors?.[0]?.imageUrls?.[0]) || '/images/default.png' %>"
                    width="258" height="313"
                    alt="<%= item.name %>"
                    class="pc__img pc__img-second"
                  >
                  <% 
                    const st = item.productStatus?.statusName;
                    const frameSrc =
                      st === 'Bán chạy' ? '/mixishop/images/frame_banchay.png' :
                      st === 'Trending' ? '/mixishop/images/frame_trending.png' :
                      '/mixishop/images/frame_new.png';
                  %>
                  <img class="category-banner__frame" src="<%= frameSrc %>" alt="Status Frame">
                </a>
              </div>

              <div class="d-flex align-items-center mt-1 custom-swatch-container">
                <% (product.colors || []).forEach(function(productColor){ 
                    const img0 = (productColor.imageUrls && productColor.imageUrls[0]) || '';
                    const title = productColor.color_id?.color_name || productColor.color || 'Màu';
                    const dataVal = productColor.color_id?.color_name || productColor.color || '';
                %>
                  <a href="#"
                    class="swatch-color custom-swatch-color"
                    style="background-image:url('<%= img0 %>'); background-size:cover;"
                    data-images="<%= (productColor.imageUrls || []).slice(0,3).join(',') %>"
                    onclick="changeColor(this); updateColorSelection(this); return false;"
                    title="<%= title %>"
                    data-value="<%= dataVal %>">
                  </a>
                <% }) %>

              </div>

              <div class="pc__info position-relative d-flex flex-column justify-content-between">
                <h1 class="pc__title">
                  <a href="/product_detail/<%= item.id || item._id %>"><%= item.name %></a>
                </h1>

                <div class="product-card__price d-flex align-items-center justify-content-between">
                  <span class="money price-old">
                    <%= formatPrice((Number(item.price) || 0) + 100000) %>
                  </span>
                  <span class="money price text-secondary small-price" style="color: blue;">
                    <%= formatPrice(item.price) %>
                  </span>
                </div>
              </div>

              <div class="anim_appear-bottom position-absolute bottom-0 start-0 d-none d-sm-flex align-items-center bg-body">
                <button class="btn-icon btn-link btn-link_lg me-4 fw-medium js-open-aside" data-aside="cartDrawer" title="Add To Cart">
                  <svg width="16" height="16" viewBox="0 0 24 24" fill="currentColor" xmlns="http://www.w3.org/2000/svg"><use href="#icon_cart" /></svg>
                  <span class="ms-2 small-text">Thêm giỏ hàng</span>
                </button>
                <a class="btn-icon btn-link btn-link_lg me-4 fw-medium" href="/product_detail/<%= item.id || item._id %>" title="Quick View">
                  <svg width="16" height="16" viewBox="0 0 24 24" fill="currentColor" xmlns="http://www.w3.org/2000/svg"><use href="#icon_view" /></svg>
                  <span class="ms-2 small-text">Chi tiết</span>
                </a>
                <button class="pc__btn-wl bg-transparent border-0 js-add-wishlist" title="Thêm yêu thích" data-product="<%= item.id || item._id %>">
                  <svg width="18" height="18" viewBox="0 0 24 24" fill="currentColor" xmlns="http://www.w3.org/2000/svg"><use href="#icon_heart" /></svg>
                </button>
              </div>
            </div>
          <% }) %>
        </div>
      </div>

      <div class="products-carousel__prev position-absolute top-50 d-flex align-items-center justify-content-center">
        <svg width="25" height="25" viewBox="0 0 25 25" xmlns="http://www.w3.org/2000/svg"><use href="#icon_prev_md" /></svg>
      </div>
      <div class="products-carousel__next position-absolute top-50 d-flex align-items-center justify-content-center">
        <svg width="25" height="25" viewBox="0 0 25 25" xmlns="http://www.w3.org/2000/svg"><use href="#icon_next_md" /></svg>
      </div>
      <div class="products-pagination mt-4 mb-5 d-flex align-items-center justify-content-center"></div>
    </div>
  </section>

  </main>

  <div class="mb-5 pb-xl-5"></div>


  <!-- Mobile Fixed Footer -->

  <!-- Filters Drawer -->
  <div class="aside-filters aside aside_right" id="shopFilter">
      <div class="aside-header d-flex align-items-center">
          <h3 class="text-uppercase fs-6 mb-0">Filter By</h3>
          <button class="btn-close-lg js-close-aside btn-close-aside ms-auto"></button>
      </div><!-- /.aside-header -->
      <div class="aside-content">
          <div class="accordion" id="categories-list">
              <div class="accordion-item mb-4">
                  <h5 class="accordion-header" id="accordion-heading-1">
                      <button class="accordion-button p-0 border-0 fs-5 text-uppercase" type="button" data-bs-toggle="collapse" data-bs-target="#accordion-filter-1" aria-expanded="true" aria-controls="accordion-filter-1">
                          Product Categories
                          <svg class="accordion-button__icon" viewBox="0 0 14 14"><g aria-hidden="true" stroke="none" fill-rule="evenodd"><path class="svg-path-vertical" d="M14,6 L14,8 L0,8 L0,6 L14,6"></path><path class="svg-path-horizontal" d="M14,6 L14,8 L0,8 L0,6 L14,6"></path></g></svg>
                      </button>
                  </h5>
                  <div id="accordion-filter-1" class="accordion-collapse collapse show border-0" aria-labelledby="accordion-heading-1" data-bs-parent="#categories-list">
                      <div class="accordion-body px-0 pb-0">
                          <ul class="list list-inline row row-cols-2 mb-0">
                              <li class="list-item"><a href="#" class="menu-link py-1">Dresses</a></li>
                              <li class="list-item"><a href="#" class="menu-link py-1">Shorts</a></li>
                              <li class="list-item"><a href="#" class="menu-link py-1">Sweatshirts</a></li>
                              <li class="list-item"><a href="#" class="menu-link py-1">Swimwear</a></li>
                              <li class="list-item"><a href="#" class="menu-link py-1">Jackets</a></li>
                              <li class="list-item"><a href="#" class="menu-link py-1">T-Shirts & Tops</a></li>
                              <li class="list-item"><a href="#" class="menu-link py-1">Jeans</a></li>
                              <li class="list-item"><a href="#" class="menu-link py-1">Trousers</a></li>
                              <li class="list-item"><a href="#" class="menu-link py-1">Men</a></li>
                              <li class="list-item"><a href="#" class="menu-link py-1">Jumpers & Cardigans</a></li>
                          </ul>
                      </div>
                  </div>
              </div><!-- /.accordion-item -->
          </div><!-- /.accordion-item -->
          <div class="accordion" id="color-filters">
              <div class="accordion-item mb-4">
                  <h5 class="accordion-header" id="accordion-heading-11">
                      <button class="accordion-button p-0 border-0 fs-5 text-uppercase" type="button" data-bs-toggle="collapse" data-bs-target="#accordion-filter-2" aria-expanded="true" aria-controls="accordion-filter-2">
                          Color
                          <svg class="accordion-button__icon" viewBox="0 0 14 14"><g aria-hidden="true" stroke="none" fill-rule="evenodd"><path class="svg-path-vertical" d="M14,6 L14,8 L0,8 L0,6 L14,6"></path><path class="svg-path-horizontal" d="M14,6 L14,8 L0,8 L0,6 L14,6"></path></g></svg>
                      </button>
                  </h5>
                  <div id="accordion-filter-2" class="accordion-collapse collapse show border-0" aria-labelledby="accordion-heading-11" data-bs-parent="#color-filters">
                      <div class="accordion-body px-0 pb-0">
                          <div class="d-flex flex-wrap">
                              <a href="#" class="swatch-color js-filter" style="color: #0a2472"></a>
                              <a href="#" class="swatch-color js-filter" style="color: #d7bb4f"></a>
                              <a href="#" class="swatch-color js-filter" style="color: #282828"></a>
                              <a href="#" class="swatch-color js-filter" style="color: #b1d6e8"></a>
                              <a href="#" class="swatch-color js-filter" style="color: #9c7539"></a>
                              <a href="#" class="swatch-color js-filter" style="color: #d29b48"></a>
                              <a href="#" class="swatch-color js-filter" style="color: #e6ae95"></a>
                              <a href="#" class="swatch-color js-filter" style="color: #d76b67"></a>
                              <a href="#" class="swatch-color swatch_active js-filter" style="color: #bababa"></a>
                              <a href="#" class="swatch-color js-filter" style="color: #bfdcc4"></a>
                          </div>
                      </div>
                  </div>
              </div><!-- /.accordion-item -->
          </div><!-- /.accordion -->
          <div class="accordion" id="size-filters">
              <div class="accordion-item mb-4">
                  <h5 class="accordion-header" id="accordion-heading-size">
                      <button class="accordion-button p-0 border-0 fs-5 text-uppercase" type="button" data-bs-toggle="collapse" data-bs-target="#accordion-filter-size" aria-expanded="true" aria-controls="accordion-filter-size">
                          Sizes
                          <svg class="accordion-button__icon" viewBox="0 0 14 14"><g aria-hidden="true" stroke="none" fill-rule="evenodd"><path class="svg-path-vertical" d="M14,6 L14,8 L0,8 L0,6 L14,6"></path><path class="svg-path-horizontal" d="M14,6 L14,8 L0,8 L0,6 L14,6"></path></g></svg>
                      </button>
                  </h5>
                  <div id="accordion-filter-size" class="accordion-collapse collapse show border-0" aria-labelledby="accordion-heading-size" data-bs-parent="#size-filters">
                      <div class="accordion-body px-0 pb-0">
                          <div class="d-flex flex-wrap">
                              <a href="#" class="swatch-size btn btn-sm btn-outline-light mb-3 me-3 js-filter">XS</a>
                              <a href="#" class="swatch-size btn btn-sm btn-outline-light mb-3 me-3 js-filter">S</a>
                              <a href="#" class="swatch-size btn btn-sm btn-outline-light mb-3 me-3 js-filter">M</a>
                              <a href="#" class="swatch-size btn btn-sm btn-outline-light mb-3 me-3 js-filter">L</a>
                              <a href="#" class="swatch-size btn btn-sm btn-outline-light mb-3 me-3 js-filter">XL</a>
                              <a href="#" class="swatch-size btn btn-sm btn-outline-light mb-3 me-3 js-filter">XXL</a>
                          </div>
                      </div>
                  </div>
              </div><!-- /.accordion-item -->
          </div><!-- /.accordion -->
          <div class="accordion" id="brand-filters">
              <div class="accordion-item mb-4">
                  <h5 class="accordion-header" id="accordion-heading-brand">
                      <button class="accordion-button p-0 border-0 fs-5 text-uppercase" type="button" data-bs-toggle="collapse" data-bs-target="#accordion-filter-brand" aria-expanded="true" aria-controls="accordion-filter-brand">
                          Brands
                          <svg class="accordion-button__icon" viewBox="0 0 14 14"><g aria-hidden="true" stroke="none" fill-rule="evenodd"><path class="svg-path-vertical" d="M14,6 L14,8 L0,8 L0,6 L14,6"></path><path class="svg-path-horizontal" d="M14,6 L14,8 L0,8 L0,6 L14,6"></path></g></svg>
                      </button>
                  </h5>
                  <div id="accordion-filter-brand" class="accordion-collapse collapse show border-0" aria-labelledby="accordion-heading-brand" data-bs-parent="#brand-filters">
                      <div class="search-field multi-select accordion-body px-0 pb-0">
                          <select class="d-none" multiple name="total-numbers-list">
                              <option value="1">Adidas</option>
                              <option value="2">Balmain</option>
                              <option value="3">Balenciaga</option>
                              <option value="4">Burberry</option>
                              <option value="5">Kenzo</option>
                              <option value="5">Givenchy</option>
                              <option value="5">Zara</option>
                          </select>
                          <div class="search-field__input-wrapper mb-3">
                              <input type="text" name="search_text" class="search-field__input form-control form-control-sm border-light border-2" placeholder="SEARCH">
                          </div>
                          <ul class="multi-select__list list-unstyled">
                              <li class="search-suggestion__item multi-select__item text-primary js-search-select js-multi-select">
                                  <span class="me-auto">Adidas</span>
                                  <span class="text-secondary">2</span>
                              </li>
                              <li class="search-suggestion__item multi-select__item text-primary js-search-select js-multi-select">
                                  <span class="me-auto">Balmain</span>
                                  <span class="text-secondary">7</span>
                              </li>
                              <li class="search-suggestion__item multi-select__item text-primary js-search-select js-multi-select">
                                  <span class="me-auto">Balenciaga</span>
                                  <span class="text-secondary">10</span>
                              </li>
                              <li class="search-suggestion__item multi-select__item text-primary js-search-select js-multi-select">
                                  <span class="me-auto">Burberry</span>
                                  <span class="text-secondary">39</span>
                              </li>
                              <li class="search-suggestion__item multi-select__item text-primary js-search-select js-multi-select">
                                  <span class="me-auto">Kenzo</span>
                                  <span class="text-secondary">95</span>
                              </li>
                              <li class="search-suggestion__item multi-select__item text-primary js-search-select js-multi-select">
                                  <span class="me-auto">Givenchy</span>
                                  <span class="text-secondary">1092</span>
                              </li>
                              <li class="search-suggestion__item multi-select__item text-primary js-search-select js-multi-select">
                                  <span class="me-auto">Zara</span>
                                  <span class="text-secondary">48</span>
                              </li>
                          </ul>
                      </div>
                  </div>
              </div><!-- /.accordion-item -->
          </div><!-- /.accordion -->
          <div class="accordion" id="price-filters">
              <div class="accordion-item mb-4">
                  <h5 class="accordion-header mb-2" id="accordion-heading-price">
                      <button class="accordion-button p-0 border-0 fs-5 text-uppercase" type="button" data-bs-toggle="collapse" data-bs-target="#accordion-filter-price" aria-expanded="true" aria-controls="accordion-filter-price">
                          Price
                          <svg class="accordion-button__icon" viewBox="0 0 14 14"><g aria-hidden="true" stroke="none" fill-rule="evenodd"><path class="svg-path-vertical" d="M14,6 L14,8 L0,8 L0,6 L14,6"></path><path class="svg-path-horizontal" d="M14,6 L14,8 L0,8 L0,6 L14,6"></path></g></svg>
                      </button>
                  </h5>
                  <div id="accordion-filter-price" class="accordion-collapse collapse show border-0" aria-labelledby="accordion-heading-price" data-bs-parent="#price-filters">
                      <input class="price-range-slider" type="text" name="price_range" value="" data-slider-min="10" data-slider-max="1000" data-slider-step="5" data-slider-value="[250,450]" data-currency="$">
                      <div class="price-range__info d-flex align-items-center mt-2">
                          <div class="me-auto">
                              <span class="text-secondary">Min Price: </span>
                              <span class="price-range__min">$250</span>
                          </div>
                          <div>
                              <span class="text-secondary">Max Price: </span>
                              <span class="price-range__max">$450</span>
                          </div>
                      </div>
                  </div>
              </div><!-- /.accordion-item -->
          </div><!-- /.accordion -->
          <div class="filter-active-tags pt-2">
              <button class="filter-tag d-inline-flex align-items-center mb-3 me-3 text-uppercase js-filter">
                  <i class="btn-close-xs d-inline-block"></i>
                  <span class="ms-2">Blues</span>
              </button>
              <button class="filter-tag d-inline-flex align-items-center mb-3 me-3 text-uppercase js-filter">
                  <i class="btn-close-xs d-inline-block"></i>
                  <span class="ms-2">Max Price: $493</span>
              </button>
              <button class="filter-tag d-inline-flex align-items-center mb-3 text-uppercase js-filter">
                  <i class="btn-close-xs d-inline-block"></i>
                  <span class="ms-2">Zara</span>
              </button>
              <div>
                  <button class="filter-tag d-flex align-items-center text-uppercase js-filter">
                      <i class="btn-close-xs d-inline-block"></i>
                      <span class="ms-2">RESET FILTER</span>
                  </button>
              </div>
          </div>
      </div><!-- /.aside-content -->
  </div><!-- /.aside-filters aside aside_right overflow-auto -->

  <%- include('partials/footer') %>
  <%- include('partials/icons') %>
  <%- include('partials/login_form') %>
  <%- include('partials/cart_drawer') %>
  <%- include('partials/quick_view') %>
  <%- include('partials/sitemap') %>

  <!-- Go To Top -->
  <div id="scrollTop" class="visually-hidden end-0"></div>

  <!-- Sizeguide -->
  <div class="modal fade" id="sizeGuide" tabindex="-1" aria-hidden="true">
      <div class="modal-dialog size-guide">
          <div class="modal-content">
              <div class="modal-header">
                  <h5 class="modal-title">Bảng kích thước</h5>
                  <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
              </div>
              <div class="modal-body">
                  <div class="size-guide__wrapper">
                      <div class="size-guide__image">
                          <img loading="lazy" src="/mixishop/images/checksize.png" alt="">
                      </div>
                  </div>
              </div>
          </div>
      </div><!-- /.modal-dialog -->
  </div>

  <!-- Page Overlay -->
  <div class="page-overlay"></div><!-- /.page-overlay -->

  <!-- External JavaScripts -->
  <script src="/mixishop/js/plugins/jquery.min.js"></script>
  <script src="/mixishop/js/plugins/bootstrap.bundle.min.js"></script>
  <script src="/mixishop/js/plugins/bootstrap-slider.min.js"></script>
  <script src="/mixishop/js/plugins/swiper.min.js"></script>
  <script src="/mixishop/js/plugins/countdown.js"></script>
  <script src="/mixishop/js/plugins/jquery.fancybox.js"></script>

  <!-- Footer Scripts -->
  <script src="/mixishop/js/theme.js"></script>

  <!-- Socket.IO client -->
  <script src="/socket.io/socket.io.js"></script>

  <script>
// Map "<sizeId>:<colorId>" -> "SKU"
window.__VAR_MATRIX = <%- JSON.stringify(typeof variantMatrix !== 'undefined' ? variantMatrix : {}) %>;
</script>


  <script>
  function _paintDebug() {
    const dbgColor = document.getElementById('dbgColor');
    const dbgSize  = document.getElementById('dbgSize');
    const colorVal = document.getElementById('selectedColor')?.value || '';
    const sizeVal  = document.getElementById('sizeName')?.value || '';
    if (dbgColor) dbgColor.textContent = colorVal || '—';
    if (dbgSize)  dbgSize.textContent  = sizeVal  || '—';
  }

  (function(){
    const productId = '<%= product._id %>';
    const loggedIn = <%= loggedInUser ? 'true' : 'false' %>;

    // ========== SOCKET ==========
    const socket = window.io ? io() : null;

    if (socket) {
      socket.on('new-comment', ({ productId: pid, comment }) => {
        if (pid !== productId) return;
        // Ưu tiên UI mới (thread + tim). Nếu chưa có, dùng tạm UI cũ.
        if (window.insertComment) window.insertComment(comment);
        else appendComment(comment);
      });

      socket.on('rating-updated', ({ productId: pid, stats }) => {
        if (pid === productId) updateRatingSummary(stats);
      });
    }

    // ========== BÌNH LUẬN ==========
    const btnSend = document.getElementById('btnSendComment');
    const inputName = document.getElementById('cmtName');
    const inputMsg  = document.getElementById('cmtMessage');
    const listEl    = document.getElementById('qaList');

    function appendComment(c){
      // nếu đang có “chưa có bình luận”, bỏ nó đi
      const emptyItem = listEl.querySelector('.text-muted');
      if (emptyItem) emptyItem.remove();
      const li = document.createElement('li');
      li.className = 'cmt-item py-2 border-bottom';
      li.innerHTML = `
        <div class="small text-muted">
          <strong>${escapeHtml(c.user)}</strong> • ${new Date(c.createdAt).toLocaleString()}
        </div>
        <div>${escapeHtml(c.message)}</div>
      `;
      listEl.prepend(li);
    }

    function escapeHtml(str){
      return (str || '').replaceAll('&','&amp;').replaceAll('<','&lt;').replaceAll('>','&gt;');
    }

    if (btnSend) {
      btnSend.addEventListener('click', async () => {
        const message = (inputMsg.value || '').trim();
        const name = (inputName.value || '').trim();
        if (message.length < 2) {
          alert('Nội dung quá ngắn');
          return;
        }
        try {
          const res = await fetch(`/api/products/${productId}/comments`, {
            method: 'POST',
            headers: { 'Content-Type': 'application/json' },
            body: JSON.stringify({ name, message })
          });
          const data = await res.json();
          if (!res.ok) throw new Error(data.error || 'Lỗi gửi bình luận');
          // tự hiển thị ngay nếu server không phát socket
          if (!socket) appendComment(data.comment);
          inputMsg.value = '';
        } catch(err){
          alert(err.message || 'Đã có lỗi xảy ra');
        }
      });
    }

    // ========== ĐÁNH GIÁ SAO ==========
    const ratingBox = document.getElementById('ratingBox');
    const summaryWrap = document.getElementById('ratingSummary');
    const ratingAvgEl = document.getElementById('ratingAvg');
    const ratingCountEl = document.getElementById('ratingCount');

    function paintStars(el, value){
      el.querySelectorAll('.rate-star, .star').forEach(st => {
        const s = parseInt(st.getAttribute('data-star') || '0', 10);
        if (s && s <= value) st.classList.add('filled'); else st.classList.remove('filled');
      });
    }

    function updateRatingSummary(stats){
      const rounded = Math.round(stats.avg || 0);
      // làm lại 5 sao trong summary
      if (summaryWrap) {
        summaryWrap.innerHTML = '';
        for (let i=1;i<=5;i++){
          const iEl = document.createElement('i');
          iEl.className = 'star' + (i<=rounded ? ' filled' : '');
          iEl.textContent = '★';
          iEl.setAttribute('data-star', i);
          summaryWrap.appendChild(iEl);
        }
      }
      if (ratingAvgEl) ratingAvgEl.textContent = (stats.avg || 0).toFixed(1);
      if (ratingCountEl) ratingCountEl.textContent = stats.count || 0;
    }

    if (ratingBox) {
      ratingBox.querySelectorAll('.rate-star').forEach(star => {
        star.addEventListener('mouseenter', () => {
          paintStars(ratingBox, parseInt(star.dataset.star,10));
        });
        star.addEventListener('mouseleave', () => {
          paintStars(ratingBox, parseInt(ratingBox.dataset.current || '0', 10));
        });
        star.addEventListener('click', async () => {
          if (!loggedIn) {
            if (confirm('Bạn cần đăng nhập để đánh giá. Chuyển tới trang đăng nhập?')) {
              window.location.href = '/login-register';
            }
            return;
          }
          const stars = parseInt(star.dataset.star,10);
          try {
            const res = await fetch(`/api/products/${productId}/ratings`, {
              method: 'POST',
              headers: { 'Content-Type': 'application/json' },
              body: JSON.stringify({ stars })
            });
            const data = await res.json();
            if (!res.ok) throw new Error(data.error || 'Lỗi đánh giá');
            // cập nhật sao của người dùng
            ratingBox.dataset.current = String(stars);
            paintStars(ratingBox, stars);
            // nếu không có socket, tự update tổng quan
            if (!socket) updateRatingSummary(data.stats);
          } catch(err){
            alert(err.message || 'Đã có lỗi xảy ra');
          }
        });
      });
      // tô lại theo đánh giá hiện tại
      paintStars(ratingBox, parseInt(ratingBox.dataset.current || '0', 10));
    }
  })();
  </script>

  <script>
  (function(){
    const pid = '<%= product._id %>';
    const isLoggedIn = <%= loggedInUser ? 'true' : 'false' %>;

    const socket = window.io ? io() : null;
    const listEl = document.getElementById('qaList');

    // Tạo node bình luận
    function createCommentNode(c){
      const li = document.createElement('li');
      li.className = 'comment-item mb-3';
      li.dataset.id = c.id;
      li.dataset.parent = c.parentId || '';
      li.innerHTML = `
        <div class="chat-bubble">
          <div class="chat-meta mb-1">${new Date(c.createdAt).toLocaleString('vi-VN')}</div>
          <div><strong>${escapeHtml(c.user)}</strong></div>
          <div class="mb-2">${escapeHtml(c.message)}</div>
          <div class="d-flex align-items-center gap-3">
            <button class="btn btn-link btn-xxs p-0 reply-btn" data-id="${c.id}">Trả lời</button>
            <button class="like-btn" data-id="${c.id}">
              <span class="heart">🤍</span>
              <span class="likes-count">${c.likes || 0}</span>
            </button>
          </div>
          <form class="reply-form mt-2 d-none" data-parent="${c.id}">
            ${!isLoggedIn ? '<input class="form-control form-control-sm mb-2" name="name" placeholder="Tên (tuỳ chọn)">' : ''}
            <textarea class="form-control form-control-sm mb-2" name="message" placeholder="Viết trả lời..." required></textarea>
            <button class="btn btn-primary btn-xxs">Gửi</button>
          </form>
        </div>
        <ul class="children list-unstyled ms-3 mt-2"></ul>
      `;
      return li;
    }

    function insertComment(c){
      const node = createCommentNode(c);
      if (c.parentId) {
        const parentLi = listEl.querySelector(`.comment-item[data-id="${c.parentId}"]`);
        const children = parentLi?.querySelector(':scope > .children');
        (children || listEl).prepend(node);
      } else {
        listEl.prepend(node);
      }
      // bỏ dòng "Chưa có bình luận..."
      const empty = listEl.querySelector('.text-muted'); if (empty) empty.remove();
    }

    // Cho khối JS phía trên có thể gọi
    window.insertComment = insertComment;

    function escapeHtml(s){return (s||'').replace(/[&<>"']/g,m=>({ '&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;' }[m]));}

    // Toggle form trả lời + thả tim
    document.addEventListener('click', async (e) => {
      const replyBtn = e.target.closest('.reply-btn');
      if (replyBtn) {
        const li = replyBtn.closest('.comment-item');
        const form = li.querySelector('.reply-form');
        form.classList.toggle('d-none');
        form.querySelector('textarea')?.focus();
        return;
      }
      const likeBtn = e.target.closest('.like-btn');
      if (likeBtn) {
        const cid = likeBtn.dataset.id;
        try {
          const res = await fetch(`/api/products/${pid}/comments/${cid}/like`, { method:'POST' });
          const data = await res.json();
          if (data.ok) {
            const heart = likeBtn.querySelector('.heart');
            const count = likeBtn.querySelector('.likes-count');
            heart.textContent = data.liked ? '❤️' : '🤍';
            count.textContent = data.likes;
          } else {
            alert(data.error || 'Không thể thả tim');
          }
        } catch(err){ console.error(err); }
      }
    });

    // Submit form trả lời (event delegation)
    document.addEventListener('submit', async (e) => {
      const form = e.target.closest('.reply-form');
      if (!form) return;
      e.preventDefault();
      const parentId = form.dataset.parent;
      const fd = new FormData(form);
      const payload = Object.fromEntries(fd.entries());
      payload.parentId = parentId;
      try{
        const res = await fetch(`/api/products/${pid}/comments`, {
          method:'POST', headers:{'Content-Type':'application/json'},
          body: JSON.stringify(payload)
        });
        const data = await res.json();
        if (data.ok){
          form.reset(); form.classList.add('d-none');
          if (!socket) insertComment(data.comment); // fallback nếu không dùng socket
        } else alert(data.error || 'Gửi trả lời thất bại');
      } catch(err){ console.error(err); }
    });

    
  })();
  </script>


  <script>
      function changeColor(swatch) {
      const dataImages = swatch.getAttribute('data-images');
      const images = dataImages ? dataImages.split(',') : [];

      if (images.length) {
        const mainSwiper = document.querySelector('.product-single__image .swiper-container').swiper;
        const thumbnailSwiper = document.querySelector('.product-single__thumbnail .swiper-container').swiper;
        const firstImage = images[0];
        const slides = document.querySelectorAll('.product-single__image-item img');
        let targetIndex = Array.from(slides).findIndex(slide => slide.getAttribute('src') === firstImage);
        if (targetIndex !== -1) {
          mainSwiper.slideTo(targetIndex);
          thumbnailSwiper.slideTo(targetIndex);
        }
      }

      const selectedColor = swatch.getAttribute('data-value') || '';
      const colorInput = document.getElementById('selectedColor');
      if (colorInput) colorInput.value = selectedColor;

      document.querySelectorAll('.swatch-color').forEach(c => c.classList.remove('swatch_active'));
      swatch.classList.add('swatch_active');

      console.log('[COLOR PICKED]', { selectedColor, hiddenColorValue: colorInput?.value });
      _paintDebug();
    }
  </script>

  <script>
    function updateSizeSelection(radio){
      document.getElementById('sizeId').value   = radio.getAttribute('data-size-id')   || '';
      document.getElementById('sizeName').value = radio.getAttribute('data-size-name') || '';

      recomputeSku(); // THÊM: tính lại SKU theo size+color
      console.log('[SIZE PICKED]', {
        sizeId: document.getElementById('sizeId').value,
        sizeName: document.getElementById('sizeName').value
      });
      _paintDebug();
    }

    function updateColorSelection(colorElement) {
      const colorName = colorElement.getAttribute("data-value")    || "";
      const colorId   = colorElement.getAttribute("data-color-id") || "";

      // Hiển thị tên màu (để người dùng thấy)
      const colorNameInput = document.getElementById("selectedColor");
      if (colorNameInput) colorNameInput.value = colorName;

      // Lưu _id của màu để tra SKU
      const colorIdInput = document.getElementById("colorId");
      if (colorIdInput) colorIdInput.value = colorId;

      recomputeSku(); // THÊM
      _paintDebug();
    }




  function recomputeSku(){
    const sizeId  = document.getElementById('sizeId')?.value || '';
    const colorId = document.getElementById('colorId')?.value || '';
    const key = `${sizeId}:${colorId}`;
    const sku = (window.__VAR_MATRIX && window.__VAR_MATRIX[key]) || '';

    const skuEl = document.getElementById('variantSku');
    if (skuEl) skuEl.value = sku;

    console.log('[SKU RESOLVED]', { sizeId, colorId, key, sku });
  }


      async function checkAvailability() {
          const productId = '<%= product._id %>';
          const selectedColor = document.getElementById("product-selectors-option-1").value;
          const selectedSize = document.getElementById("product-selectors-option-0").value;
          console.log("Hàm này được gọi:", document.getElementById("product-selectors-option-1").value)

          if (!selectedColor || !selectedSize) {
              document.getElementById("availabilityText").innerText = "Kiểm tra";
              document.getElementById("availabilityButton").disabled = true;
              return;
          }

          const availabilityText = document.getElementById("availabilityText");
          availabilityText.innerText = "Đang kiểm tra...";

          try {
              const response = await fetch(`/product_detail/${productId}/check-stock?color=${selectedColor}&size=${selectedSize}`);

              if (response.ok) {
                  const stockQuantity = await response.json();

                  if (stockQuantity > 0) {
                      document.getElementById("availabilityButton").disabled = false;
                      availabilityText.innerText = `Còn hàng: ${stockQuantity}`;
                  } else {
                      document.getElementById("availabilityButton").disabled = true;
                      availabilityText.innerText = "Hết hàng";
                  }
              } else {
                  availabilityText.innerText = "Không có hàng";
                  console.error("Error checking stock availability: ", response.statusText);
              }
          } catch (error) {
              availabilityText.innerText = "Không có hàng";
              console.error("Không có hàng", error);
          }
      }
  </script>

  <script>
  document.addEventListener('DOMContentLoaded', function () {
    console.log('[ON LOAD] Hidden values', {
      productId : document.getElementById('productId')?.value,
      variantSku: document.getElementById('variantSku')?.value,
      sizeId    : document.getElementById('sizeId')?.value,
      sizeName  : document.getElementById('sizeName')?.value,
      colorName : document.getElementById('selectedColor')?.value
    });
    _paintDebug();
  });

  document.addEventListener('DOMContentLoaded', function(){
    const form = document.forms['addtocart-form'] || document.querySelector('form[name="addtocart-form"]');
    if (!form) return;

    form.addEventListener('submit', function(e){
      const payload = {
        product_id : document.getElementById('productId')?.value,
        size_id    : document.getElementById('sizeId')?.value,
        color_id   : document.getElementById('colorId')?.value,
        sizeName   : document.getElementById('sizeName')?.value,
        colorName  : document.getElementById('selectedColor')?.value,
        quantity   : form.querySelector('input[name="quantity"]')?.value
        // variant_sku: document.getElementById('variantSku')?.value  // không còn bắt buộc
      };

      console.log('[SUBMIT ADD-TO-CART]', payload);

      // CHỈ kiểm tra thiếu size hoặc màu
      if (!payload.size_id || !payload.color_id) {
        e.preventDefault();
        alert('Vui lòng chọn đủ Size và Màu.');
      }
      // không chặn vì thiếu SKU nữa
    });
  });

  </script>


  <script>
      document.addEventListener("DOMContentLoaded", function() {
          const notificationModal = document.getElementById('notificationModal');
          const modalMessage = document.getElementById('modalMessage');
          const closeModal = document.getElementById('closeModal');

          const flashMessage = <%- JSON.stringify(typeof message === 'undefined' ? "" : message) %>;
  const flashError   = <%- JSON.stringify(typeof error === 'undefined' ? "" : error) %>;

          if (flashMessage || flashError) {
              modalMessage.textContent = '';
              modalMessage.classList.remove("success", "error");

              if (flashMessage) {
                  modalMessage.textContent = flashMessage;
                  modalMessage.classList.add("success");
              } else if (flashError) {
                  modalMessage.textContent = flashError;
                  modalMessage.classList.add("error");
              }
              notificationModal.style.display = "block";
          }

          closeModal.onclick = function() {
              notificationModal.style.display = "none";
          };

          window.onclick = function(event) {
              if (event.target === notificationModal) {
                  notificationModal.style.display = "none";
              }
          };
      });

      document.addEventListener("DOMContentLoaded", function () {
          const firstSize = document.querySelector("input[name='size']");
          if (firstSize) {
              firstSize.checked = true;
              updateSizeSelection(firstSize);
          }

          const firstColor = document.querySelector(".swatch-color.custom-swatch-color");
          if (firstColor) {
              changeColor(firstColor);
              updateColorSelection(firstColor);
          }

          recomputeSku();
      });

      const decreaseButton = document.querySelector('.qty-control__reduce');
      const increaseButton = document.querySelector('.qty-control__increase');
      const quantityInput = document.querySelector('.qty-control__number');

      decreaseButton.addEventListener('click', function() {
          let currentValue = parseInt(quantityInput.value);
          if (currentValue > 1) {
              quantityInput.value = currentValue - 1;
          }
      });

      increaseButton.addEventListener('click', function() {
          let currentValue = parseInt(quantityInput.value);
          quantityInput.value = currentValue + 1;
      });

      // Auto select size đầu tiên khi load
      document.addEventListener('DOMContentLoaded', function(){
        const first = document.querySelector('input[name="size"]');
        if (first){
          first.checked = true;
          updateSizeSelection(first);
        }
      });



  document.getElementById('reviewFilters')?.addEventListener('click', (e)=>{
    const btn = e.target.closest('button[data-star]');
    if(!btn) return;
    document.querySelectorAll('#reviewFilters button').forEach(b=>b.classList.remove('active'));
    btn.classList.add('active');
    const star = parseInt(btn.dataset.star,10);
    document.querySelectorAll('#commentList .comment-item').forEach(li=>{
      const s = parseInt(li.dataset.star||'0',10);
      li.style.display = (!star || s===star) ? '' : 'none';
    });
  });

  </script>

  <script>
  (function(){
    const pid = '<%= product._id %>';
    const socket = window.io ? io() : null;

    const btnAsk = document.getElementById('btnAsk');
    const qaMsg  = document.getElementById('qaMessage');
    const qaList = document.getElementById('qaList');

    function escapeHtml(s){return (s||'').replace(/[&<>"']/g,m=>({ '&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;' }[m]));}

    function prependQA(item){
      // nếu đang có dòng "Chưa có câu hỏi..."
      const empty = qaList.querySelector('.text-muted'); if (empty) empty.remove();
      const li = document.createElement('li');
      li.className = 'p-3 border-bottom';
      li.dataset.id = item.id;
      li.innerHTML = `
        <div class="small text-muted">${new Date(item.createdAt).toLocaleString('vi-VN')}</div>
        <div class="fw-semibold">${escapeHtml(item.user)}</div>
        <div class="mt-1">${escapeHtml(item.message)}</div>
        <ul class="list-unstyled ms-3 mt-2"></ul>`;
      qaList.prepend(li);
    }

    btnAsk?.addEventListener('click', async ()=>{
      const msg = (qaMsg.value||'').trim(); 
      if(msg.length<3){ qaMsg.focus(); return; }
      try{
        // API: bạn có thể dùng riêng /api/products/:pid/questions
        const res = await fetch(`/api/products/${pid}/comments`,{
          method:'POST', headers:{'Content-Type':'application/json'},
          body: JSON.stringify({ message: msg })
        });
        const data = await res.json();
        if(!res.ok) throw new Error(data.error||'Không gửi được câu hỏi');
        if(!socket) prependQA(data.comment);  // fallback nếu không dùng socket
        qaMsg.value='';
      }catch(err){ alert(err.message); }
    });

    // realtime nhận câu hỏi mới
  })();
  </script>



  </body>

  <!-- Mirrored from uomo-html.flexkitux.com/Demo3/product2_variable.html by HTTrack Website Copier/3.x [XR&CO'2014], Sat, 19 Oct 2024 06:54:06 GMT -->
  </html>